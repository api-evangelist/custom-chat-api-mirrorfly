# Custom Chat API | MirrorFly

MirrorFly is a self-hosted CPaaS providing token-authenticated REST Chat Platform APIs (User and Admin) plus SDKs for in-app chat, HD video/voice calling, VoIP, contact center, chatbot/conversational AI, and live streaming for web and mobile apps.

- **Provider:** https://www.mirrorfly.com/
- **Documentation:** https://www.mirrorfly.com/docs/
- **Base URL:** —
- **Tags:** Chat API, Messaging, CPaaS, VoIP, Voice, Video Calling, Contact Center, Conversational AI, Chatbot, Live Streaming
- **Public API:** yes
- **Agent-native (MCP / llms.txt / skills):** yes
- **Confidence:** 80/100

## APIs

- **MirrorFly Chat Platform API** — Token-authenticated REST API for building in-app chat, split into User APIs and Admin APIs (users, contacts, recent/history chat, groups, media, metadata, presence, block/unblock, call logs, device token). Uses versioned /api/v1/ paths against a customer-specific instance base URL; bearer token valid up to 1 hour. (`https://{api-base-url}/api/v1`)

> REST API is documented (Docusaurus, per-endpoint curl examples) with username/password to short-lived bearer token auth; secured endpoints use Authorization: AuthToken header. No single public base URL — MirrorFly is self-hosted, so requests target a customer's own instance (https://{api-base-url}/api/v1/). An llms.txt is declared at the well-known location per scan (body contents not independently verified). No hosted MCP server, no published agent skills, and no machine-readable contract (OpenAPI/AsyncAPI/GraphQL/Postman) were found. The advertised apis.json URL (https://www.mirrorfly.com/apis.json) returns the site's 404 page, so submitted metadata appears submitter-supplied rather than provider-hosted.

_Generated by apis.io from a community submission._